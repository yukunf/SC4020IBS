# 全部代码文件执行说明

## Data preparation
执行需要以下数据文件
- inshop_clip_labels_gallery.npy
- inshop_clip_labels_query.npy
- inshop_clip_vectors_gallery.npy
- inshop_clip_vectors_query.npy
- inshop_clip_ids_gallery.json
- inshop_clip_ids_query.json

FMNIST数据集：

- fmnist_resnet50_vectors.npy
- fmnist_resnet50_labels.npy

其余都会自动生成。

数据处理源码[data_parser1.py](../../src/data_parser1.py)，[inshop_embedder.py](../../src/inshop_embedder.py)，
[inshop_evaluation.py](../../src/inshop_evaluation.py)用于从图片形式的INSHOP数据集里获得上述的数据文件并执行一些测试，如果已经有了就不需要执行了。

## LSH&BF Analysis

我直接引用赖奕的文档：


### Brute Force Search（`src/brute_force_search.py`）

这个只是库，不直接执行

### LSH Search（`src/lsh_search.py`）

这个也是库

----


### 统一评测（`src/unified_evaluation_en.py`）

**功能**：自动在两个数据集上评测多种算法配置

**运行方式**：
```bash
python src/unified_evaluation_en.py
```

**评测内容**：
1. Brute Force（基准）
2. LSH配置1（5表，8位）
3. LSH配置2（10表，10位）
4. LSH配置3（20表，12位）

**评测指标**：
- **构建时间**（Build Time）：索引构建耗时
- **平均查询时间**（Avg Query Time）：单次查询耗时
- **QPS**（Queries Per Second）：每秒查询数
- **内存占用**（Memory Usage）：算法内存占用
- **Recall@k**：召回率（同类别样本比例）
- **Accuracy@k**：准确率（与Brute Force结果的重合度）

**自定义评测**：
```python
from src.unified_evaluation_en import UnifiedEvaluator

evaluator = UnifiedEvaluator(output_dir="my_results")

results = evaluator.evaluate_all_algorithms(
    vectors_path="data/fmnist_resnet50_vectors.npy",
    labels_path="data/fmnist_resnet50_labels.npy",
    k_values=[1, 10, 50],      # 评测Top-1, Top-10, Top-50
    metric='cosine',           # 距离度量
    test_size=1000             # 查询样本数
)
```
----

### LSH参数分析（`parameter_analysis_en.py`）

**功能**：系统化分析LSH参数对性能的影响

**运行方式**：
```bash
python parameter_analysis_en.py
```

**分析内容**：
1. **参数网格搜索**：测试不同`num_tables`和`hash_size`组合
2. **可扩展性分析**：测试不同数据规模下的性能

**输出文件**：
- `doc/report/LSHBSreports/analysis/lsh_parameter_analysis_en.csv`：参数实验数据
- `doc/report/LSHBSreports/analysis/lsh_parameter_analysis.png`：参数热力图
- `doc/report/LSHBSreports/analysis/scalability_analysis_en.csv`：可扩展性数据
- `doc/report/LSHBSreports/analysis/scalability_analysis.png`：可扩展性趋势图
- `doc/report/LSHBSreports/analysis/analysis_report_en.md`：完整分析报告

---

### PCA on LSH Test(test_pca_lsh.py)

这个文件测试PCA压缩于LSH搜索的影响，会有命令行输出及测试文件csv保存。

### Visualize on PCA(visualize_pca_lsh_en.py)
这个文件对上面文件的测试结果进行绘图

---
## FAISS Analysis

**数据准备**：




### Data Preparation ([FAISS_NeighborSearch.py](../../src/FAISS/FAISS_NeighborSearch.py))

`FAISS_Utils.py`负责切分INSHOP数据集，为后面使用做准备，请先执行一次。


然后`FAISS_NeighborSearch.py`会准备好index然后进行一次简单的Sanity Check（最初要做分层抽样但是实际上有点问题所以只用作Sanity Check了）—

### Hyperparameter Grid Search ([FAISS_analysis.py](../../src/FAISS/FAISS_analysis.py))

这个文件负责执行对两个数据集的Grid Search，找到IVF Index使用的合适的Hyperparameter，并记录结果至csv，同时画图。

### Grid Optimizer ([FAISS_GridOptimizer.py](../../src/FAISS/FAISS_GridOptimizer.py))

这个文件读取上文件产生的csv并通过加权平均优化出一些合适的超参数搭配。